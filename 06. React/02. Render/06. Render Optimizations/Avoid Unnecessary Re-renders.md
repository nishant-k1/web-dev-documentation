## Avoid unnecesary use of useContext hooks

## use React.memo or React.pureCompoent to avoid unnecesary re-renderso of down the compoent tree re-renders

## Avoid Prop Drilling:

## Use Context API or global state management to pass data across deeply nested components.

# Diffing

Diffing is the comparison step during reconciliation. It works **between two in-memory trees** (e.g. current Fiber tree vs work-in-progress Fiber tree). React never diffs against the real DOM.
It always happens between two in-memory trees.

.

🔹 Important Rule

React never diffs against the real DOM.

The real DOM is only updated during commit.

Diffing happens before any real DOM mutation.

🔹 What Actually Happens (Modern Fiber Model)

```text
Build work-in-progress Fiber tree
    ↓
Compare with current Fiber tree
    ↓
Mark updates
    ↓
Commit DOM mutations
```

**Step 1 — Build Work-In-Progress Tree**

React runs your component again and builds a new tree in memory.
This is not mutating the old tree.
It creates a separate "work-in-progress" tree.

**Step 2 — Compare With Current Tree**

While building the new tree, React compares:
Element types
Keys
Props
Children structure

This is the diffing algorithm.

This process is called reconciliation.
Still no real DOM touched.

**Step 3 — Mark Updates**
If React sees a difference, it marks the Fiber node with flags like:
Placement
Update
Deletion

These flags are stored on Fiber nodes.

**Step 4 — Commit Phase**

Now React walks the marked fibers and:
Inserts DOM nodes
Updates attributes
Removes nodes

Only here does real DOM change.

🔹 Old Mental Model vs Accurate Mental Model
Old simplified explanation:
`New vDOM → diff → patch DOM`

More precise version:

Current Fiber tree (represents UI on screen)
Work-in-progress Fiber tree (represents next UI)
Diff happens between those two
DOM mutations happen after diff is complete

🔹 Key Clarification
The real DOM is never used for comparison.
React trusts its Fiber tree as the source of truth.
Real DOM is treated as a dumb output target.

## Diffing algo works between old vDOM and new vDOM after mutation?

No mutation yet.
The new tree is built separately.

Does it compare with real DOM? No. Never.

Comparison is always:
Current Fiber tree ↔ Work-in-progress Fiber tree

```text
Render Phase:
  - Build new Fiber tree
  - Compare with current Fiber tree
  - Mark differences

Commit Phase:
  - Apply differences to real DOM
```

---

## Keys and Reconciliation

**Keys** help React identify which items in a list are the same, added, removed, or reordered across renders.

- **Why keys matter:** Without keys, React relies on **position** (index). If you insert, remove, or reorder list items, React can reuse the wrong DOM nodes and mix up state (e.g. which item is selected). With a **stable, unique key** per item (e.g. `item.id`), React can match old and new items correctly and only update what changed.
- **Where keys are used:** During diffing, when React compares children of a list, it uses the `key` prop to decide whether to reuse an existing Fiber/DOM node, update it, or remove/create it. Wrong or missing keys can cause unnecessary re-renders, wrong UI, or bugs.
- **Good keys:** Stable unique IDs from your data. **Avoid index as key** when the list can be reordered, filtered, or items added/removed in the middle.

So: keys directly affect how the diffing algorithm reconciles list children and updates the real DOM.

---

## 🧠 Does React First Build a Full New vDOM, Then Diff?

Short answer: **No.**  
That mental model is slightly outdated.

React does **not**:

- Build a complete new virtual DOM tree
- Then run a separate diff pass afterward

That was the simplified explanation used years ago.

---

## 🔥 What Actually Happens (Fiber Architecture)

In modern React (Fiber):

> Reconciliation happens incrementally during rendering.

It is **not**:

```
Create full new tree → Then compare
```

It is more like:

```
For each element during render:
   Compare with previous Fiber
   Decide reuse or replace
   Build new Fiber node
```

So diffing is happening **as React walks the tree**.

---

## 🧠 How It Really Works (Simplified)

When React renders:

1. It starts at the root.
2. For each element returned:
   - It checks the previous Fiber at that position.
   - Compares `type` and `key`.
   - Decides whether to reuse or create a new node.
3. It builds a **work-in-progress Fiber tree**.

So reconciliation is integrated into the render process itself.

---

## 🔥 Visual Mental Model

```
Old Fiber Tree
        ⬇
React walks new elements one by one
        ⬇
For each node:
   - Match by type + key
   - Update props
   - Reconcile children
        ⬇
Build new Fiber tree incrementally
```

There is **no separate full diff phase afterward**.

---

## 🎯 Why This Matters

Because Fiber enables:

- Interruptible rendering
- Concurrent rendering
- Priority scheduling
- Partial tree processing

If React had to build a full new tree first, it wouldn’t be able to pause midway. That would block the main thread.

---

## 🔥 Simple Mental Model

### Old (Conceptual) Explanation

```
New vDOM → Diff → Patch DOM
```

### Fiber Model

```
Walk tree → Compare + Build → Commit changes
```

Reconciliation is **not separate from rendering**.

It is part of rendering.

---

## 🎯 Interview-Ready Explanation

If asked:

> Does React first create a full new vDOM and then diff it?

You can say:

> In the Fiber architecture, reconciliation happens during the render phase. React incrementally compares each element with the previous Fiber while building a work-in-progress tree. It does not build a complete new tree first and then run a separate diff pass.

That answer sounds mature and technically accurate.

---

## 🧠 Subtle Follow-Up Question

If reconciliation happens during the render phase,  
why are DOM updates **not applied immediately** during that phase?

Because:

- The **render phase must stay pure**
- React might pause, restart, or discard that work
- Applying DOM changes early would cause inconsistent UI
- DOM mutations are deferred to the **commit phase**

Render phase → pure computation  
Commit phase → actual DOM mutations

This separation is what makes concurrent rendering possible.

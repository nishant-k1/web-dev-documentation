# Querying within elements

- Find element within an element
- sometimes we need to test element inside an element. Like we may have span tag inside p tag we need to test the span tag
- For testing parent element along with the child element

# Promise Object Constructor

`Promise` is a built-in object for handling asynchronous operations. **Most frequently asked in interviews!**

## What is a Promise?

A Promise is an object that represents the eventual completion or failure of an asynchronous operation. It has 2 broad states:

1. **Pending**: Initial state, neither fulfilled nor rejected
2. **settled**: It has again two state: either resolved/fulfilled or rejected.
   1. **resolved/fulfilled**: Operation completed successfully and has a promise value. Fulfilled doesn't mean raw value.
   2. **Rejected**: Operation failed and has a promise reason (error) value. Reject doesn't mean raw error value.

   Both **Fulfilled** and **Rejected** states are again promise values which can be unpacked using .then or await, while Pending not value at all.

## Succeeded and Failed

**Succeeded**: This is informal language, “Succeeded” = Fulfilled
**Failed**: This is informal language, “Failed” = Rejected

## Creating Promises

### 1. Using Constructor

```js
const promise = new Promise((resolve, reject) => {
  // Async operation
  if (success) {
    resolve(value); // might return another promise or a value
  } else {
    reject(error); // Rejected
  }
});
```

### 2. Using Static Method Promise.resolve()

Creates a promise that is:

- already resolved (not fulfilled)
- usually fulfilled (unless value is a promise)

```js
Promise.resolve(2);
```

### 3. Using Static Method Promise.reject()

Creates a promise that is: already rejected

```js
Promise.reject("error");
```

### 4. Using async functions

This automatically returns a promise

```js
async function getData() {
  return 10;
}
```

Above example code is Equivalent to:

```js
function getData() {
  return Promise.resolve(10);
}
```

**There is only one way to create a promise manually — using the Promise constructor.**

We commonly create promises in 4 ways:

- new Promise() (manual creation)
- Promise.resolve()
- Promise.reject()
- async functions (implicit promises)

But Only this gives you full control:

- new Promise((resolve, reject) => {})

Everything else is just:

- wrapping
- shortcut
- or abstraction

## What is returned from the created promise?

1. **Promise Instance**
   - new Promise() (resolve => resolve(x)) → returns a Promise
   - new Promise() (reject => reject(x)) → returns a Promise

2. **Static Methods**
   - Promise.resolve() → returns a Promise
   - Promise.reject() → returns a Promise
   - Promise.all() -> return a Promise
   - Promise.any() -> return a Promise
   - Promise.allSettled() -> return a Promise
   - Promise.race() -> return a Promise
   - async functions (implicit promises) → returns a Promise

✅ Always a Promise

👉 Never returns a raw value

All promise static methods Promise.resolve(), Promise.reject(), Promise.all(), Promise.any(), Promise.allSettled(), Promise.race() as well Promise Instances - all return a promise object immediately. Not a raw value. A promise object. Only after you unwrap it with .then(), .catch(), or await do you get the actual raw value or error. So yes, every single one returns a promise object.

## How to access the actual value from the promise value

Both fulfilled and rejected cases always return a promise object; the value or error is accessed using .then method, .catch method, or await.

## Mental model

1. Promise → box
2. Fulfilled → box has value
3. Rejected → box has error
4. .then() / .catch() / await → open the box

## Promise Static Methods

1. all()
2. allSettled()
3. any()
4. race()
5. reject()
6. resolve()
7. try()
8. withResolvers()

## Instance methods

then()
catch()
finally()

## Common Patterns

### Timeout Pattern

```js
function withTimeout(promise, timeoutMs) {
  const timeout = new Promise((_, reject) =>
    setTimeout(() => reject(new Error("Timeout")), timeoutMs),
  );
  return Promise.race([promise, timeout]);
}
```

### Parallel Execution

```js
// Execute multiple promises in parallel
const [users, posts, comments] = await Promise.all([
  fetchUsers(),
  fetchPosts(),
  fetchComments(),
]);
```

### Sequential Execution

```js
// Execute promises sequentially
const user = await fetchUser();
const posts = await fetchUserPosts(user.id);
const comments = await fetchPostComments(posts[0].id);
```

### Promise Chaining

Chaining `.then()` methods to handle sequences of async operations.

```js
fetch("/api/user")
  .then((response) => response.json())
  .then((user) => {
    return fetch(`/api/posts/${user.id}`);
  })
  .then((response) => response.json())
  .then((posts) => {
    console.log(posts);
  })
  .catch((error) => {
    console.error("Error in chain:", error);
  });
```

### Handle errors in Promise chains

Use `.catch()` at the end of the chain. Errors propagate down the chain until caught. After `.catch()`, the chain can continue if you return a value.

```js
promise
  .then(step1)
  .then(step2)
  .catch((error) => {
    // Catches errors from step1 or step2
    return defaultValue; // Chain continues
  })
  .then(step3); // Executes after catch if catch returns value
```

### Handle errors in async await chains

Use try catch block

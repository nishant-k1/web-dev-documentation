console.log(a);
var a = 10;

// a declaration gets hoisted
// a initialization does not get hoisted
// a gets default initilazaiton of undefined as it has been declared using var keyword
// log is undefined

# Control Flow Statements

Control the flow of program execution.

## 1. Conditional Statements

### if statement

```js
if (condition) {
  // code
}
```

### if-else statement

```js
if (condition) {
  // code
} else {
  // code
}
```

### if-else-if statement

```js
if (condition1) {
  // code
} else if (condition2) {
  // code
} else {
  // code
}
```

### switch statement

```js
switch (value) {
  case 1:
    // code
    break;
  case 2:
    // code
    break;
  default:
  // code
}
```

## 2. Loop Statements

### for loop

```js
for (let i = 0; i < 10; i++) {
  // code
}
```

### while loop

```js
while (condition) {
  // code
}
```

### do-while loop

```js
do {
  // code
} while (condition);
```

### for...in loop

Iterates over object properties.

```js
for (let key in object) {
  // code
}
```

### for...of loop

Iterates over iterable values (arrays, strings, etc.).

```js
for (let value of array) {
  // code
}
```

### for-await-of loop

Iterates over async iterables (async generators, async iterators). Introduced in ES2018.

```js
for await (let value of asyncIterable) {
  // code
}
```

**Key characteristics:**

- Works with async iterables (objects with `Symbol.asyncIterator`)
- Can only be used inside async functions or at the top level of modules
- Awaits each value as it's yielded
- Used with async generators, async iterators, and other async iterables

**Examples:**

```js
// With async generator
async function* asyncNumbers() {
  for (let i = 0; i < 5; i++) {
    await new Promise((resolve) => setTimeout(resolve, 100));
    yield i;
  }
}

async function processNumbers() {
  for await (const num of asyncNumbers()) {
    console.log(num); // 0, 1, 2, 3, 4 (with delays)
  }
}

// With async iterable
async function* fetchPages() {
  for (let i = 1; i <= 3; i++) {
    const response = await fetch(`/api/page/${i}`);
    yield response.json();
  }
}

async function displayPages() {
  for await (const page of fetchPages()) {
    console.log(page);
  }
}
```

## 3. Loop Control Statements

Control the execution of loops.

### break

Exits the loop immediately.

```js
for (let i = 0; i < 10; i++) {
  if (i === 5) break; // exits loop when i is 5
}
```

### continue

Skips to the next iteration.

```js
for (let i = 0; i < 10; i++) {
  if (i === 5) continue; // skips iteration when i is 5
}
```

## Important Notes

- **Conditional statements** allow branching based on conditions
- **Loop statements** allow repetitive execution
- **Loop control statements** (`break`, `continue`) modify loop behavior
- `break` and `continue` can also be used with labels (see [Label Statements](./07.%20Label%20Statements.md))

# ✅ Standard Built-in JavaScript Objects

JavaScript provides several built-in global objects you can use without importing anything. Understanding these is **crucial** for both frontend interviews and problem-solving rounds.

> 📚 **New to built-in objects?** Check out [00. STUDY_ORDER.md](./00.%20STUDY_ORDER.md) for the recommended study order based on interview importance and real-world web development needs.

---

## 🧱 1. Core Objects

### 🔹 `Object`

- Base of all JS objects.
- Used to create simple objects or extend functionality.

**Must-Know Methods:**

```js
Object.keys(obj);
Object.values(obj);
Object.entries(obj);
Object.assign(target, source);
Object.create(proto);
Object.hasOwn(obj, prop);
Object.freeze(obj);
Object.seal(obj);
```

---

### 🔹 `Function`

- Every function in JS is an instance of `Function`.

**Must-Know:**

```js
func.call(thisArg, ...args);
func.apply(thisArg, [args]);
func.bind(thisArg);
func.name;
func.length;
```

---

### 🔹 `Boolean`, `Number`, `String`

- Primitive wrappers, not commonly used explicitly.

**String Methods:**

```js
str.length;
str.includes(), startsWith(), endsWith();
str.indexOf(), lastIndexOf();
str.slice(), substring(), substr();
str.toUpperCase(), toLowerCase();
str.trim(), padStart(), padEnd();
str.split(), replace();
```

**Number Methods:**

```js
Number.isNaN(), isFinite();
Number.parseInt(), parseFloat();
num.toFixed(), toPrecision();
```

---

## 📅 2. Date & Time

### 🔹 `Date`

- Used for date/time manipulation.

**Must-Know:**

```js
new Date(), Date.now();
date.getFullYear(), getMonth(), getDate();
date.setFullYear(), setMonth();
date.toISOString(), toLocaleString();
```

---

## 🧮 3. Math & Numbers

### 🔹 `Math`

- Static object for math operations.

**Must-Know:**

```js
Math.round(), floor(), ceil(), trunc();
Math.random(), abs(), sqrt(), pow();
Math.max(), min();
Math.PI, Math.E;
```

---

## 🧵 4. Collection Objects

### 🔹 `Array`

- Dynamic list-like object.

**Must-Know:**

```js
arr.push(), pop(), shift(), unshift();
arr.map(), filter(), reduce(), forEach();
arr.sort(), reverse(), splice(), slice();
arr.includes(), find(), findIndex();
arr.every(), some();
```

---

### 🔹 `Set`, `WeakSet`

- `Set`: Unique values only.
- `WeakSet`: Only objects, not enumerable.

**Set Must-Know:**

```js
new Set([1, 2, 3])
set.add(), delete(), has(), clear()
```

---

### 🔹 `Map`, `WeakMap`

- `Map`: Key-value pairs (any type as key).
- `WeakMap`: Only object keys, not enumerable.

**Map Must-Know:**

```js
new Map();
map.set(k, v), get(k), has(k), delete k;
```

---

## 📜 5. JSON

### 🔹 `JSON`

- Serialize and deserialize data.

**Must-Know:**

```js
JSON.stringify(obj);
JSON.parse(jsonStr);
```

---

## 🧪 6. Regular Expressions

### 🔹 `RegExp`

- For pattern matching in strings.

**Must-Know:**

```js
/test/.test(str)
/test/.exec(str)
str.match(), str.replace(), str.search()
```

---

## 📦 7. Error Objects

### 🔹 `Error`, `TypeError`, `ReferenceError`, `SyntaxError`, etc

- For error handling and debugging.

**Must-Know:**

```js
try {
  throw new Error("message");
} catch (e) {
  console.error(e.name, e.message);
}
```

---

## 📐 8. Utility Objects

### 🔹 `Symbol`

- Unique identifiers.

```js
const sym = Symbol("desc");
```

### 🔹 `BigInt`

- For handling large integers.

```js
const big = 12345678901234567890n;
```

---

## ⚡ 9. Async Objects

### 🔹 `Promise`

- Built-in object for handling asynchronous operations.
- Represents eventual completion or failure of async tasks.

**Must-Know Static Methods:**

```js
Promise.all(promises);
Promise.race(promises);
Promise.allSettled(promises); // ES2020
Promise.any(promises); // ES2021
Promise.resolve(value);
Promise.reject(reason);
```

**Instance Methods:**

```js
promise.then(onFulfilled, onRejected);
promise.catch(onRejected);
promise.finally(onFinally);
```

📖 **Detailed Documentation:** See [29. Handling Async Js/03. Promises.md](../29.%20Handling%20Async%20Js/03.%20Promises.md) for comprehensive Promise usage, examples, and interview patterns.

---

## 🧠 Bonus: Global Functions

| Function                            | Use Case                  |
| ----------------------------------- | ------------------------- |
| `eval()`                            | Avoid in real use         |
| `isNaN()`, `isFinite()`             | Number checking           |
| `parseInt()`, `parseFloat()`        | Convert strings to number |
| `encodeURI()`, `decodeURI()`        | URL encoding/decoding     |
| `setTimeout()`, `setInterval()`     | Timers                    |
| `clearTimeout()`, `clearInterval()` | Cancel timers             |

---

## ✅ Quick Interview Checklist

- [x] `Object`, `Function`
- [x] `Array`, `Set`, `Map`
- [x] `Date`, `Math`, `JSON`
- [x] `RegExp`, `Error`
- [x] `Promise` (static methods: all, race, allSettled, any)
- [x] Global functions like `parseInt`, `setTimeout`

---

💡 **Pro Tips:**

- Focus more on _usage patterns_ over memorizing APIs.
- Practice tasks like: deep cloning, sorting objects, formatting dates, debouncing, throttling.
- Follow the [recommended study order](./00.%20STUDY_ORDER.md) to learn efficiently - start with **Promise** if preparing for interviews!

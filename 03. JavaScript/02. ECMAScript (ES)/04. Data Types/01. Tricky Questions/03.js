console.log(a); // What is logged?
let a = 10;

// a declaration gets hoisted
// a initialization doesn't get hoisted
// sind declartin is using let keword so no defalult initalization with any value.
// Accessing a variable before initilizaiton will throw Reference Error

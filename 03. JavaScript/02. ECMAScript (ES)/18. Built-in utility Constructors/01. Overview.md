# Built-in Constructors

These are **built-in constructors**: they are functions you can use with `new` (e.g. `new Array()`, `new Object()`) or via literals (`[]`, `{}`, `""`). In JavaScript, every function is an object, so they also have static methods on the constructor (e.g. `Object.keys`, `Array.isArray`).

## Contents

### Primitive Wrappers and Primitive Value Objects

1. String
2. Number
3. Boolean
4. Symbol
5. BigInit

### Collection Constructors

1. Arrays
2. Typed Arrays
3. Array Buffer
4. Map
5. Set
6. WeakSet
7. WeakMap
8. DataView

## Core Object Constructors

1. Date
2. Objects
3. Functions
4. RegExp
5. Error
6. Promise
7. Generator
8. Typed Arrays
9. ArrayBuffer
10. DataView
11. Proxy
12. WeakRef
13. FinalizationRegistry

# Web APIs

Web APIs are a broader category and can also include device-specific APIs (e.g., camera, microphone, Bluetooth).

- A much broader term encompassing all the APIs provided by the browser or platform for web development.
- The BOM is a subset of Web APIs. It’s focused on browser-specific interactions, while - Web APIs include many other features such as:

- DOM APIs: Manipulating the document (e.g., getElementById, createElement).
  - Fetch API: Handling network requests.
  - Web Storage API: LocalStorage and SessionStorage.
  - Geolocation API: Accessing location data.
  - Notifications API, WebSockets, WebRTC, and more.

# Call Stack

- The call stack is where JavaScript keeps track of function calls. It's a stack data structure where functions are pushed when they are invoked and popped once they complete.

- JavaScript executes synchronous code by adding it to the call stack and running it until it's finished.

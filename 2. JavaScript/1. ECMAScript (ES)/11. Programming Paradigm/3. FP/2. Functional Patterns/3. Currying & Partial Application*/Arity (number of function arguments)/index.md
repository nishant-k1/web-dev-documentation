# Arity

- It simply means number of arguments a function takes.
- The more parameters a function has, the harder it is to really make compose it with other functions.
- Genereally, we should not prefer more than 2 parameters.

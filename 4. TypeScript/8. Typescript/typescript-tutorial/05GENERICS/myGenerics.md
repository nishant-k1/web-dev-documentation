# Generic

- Generics in TypeScript allow you to create flexible and reusable code components where the input and output data types can vary and are determined when you use the component.
- for same input type we want same output type and the type should be decided during the function call
